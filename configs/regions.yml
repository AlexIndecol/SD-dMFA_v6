# Canonical region dimension used by configs, exogenous inputs, and scenario filters.
dimension:
  name: region
  symbol: r

# Keep names stable. These strings must match CSV values (after loader aliases).
regions:
  - EU27
  - China
  - RoW
