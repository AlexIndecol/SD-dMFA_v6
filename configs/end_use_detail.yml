# Optional mapping from detailed labels to modeled `end_uses`.
# Keep `{}` when detailed disaggregation is not active in the run.
mapping: {}

# Practical guardrail:
# if you define detailed rows, normalized shares must satisfy
# sum(detail_share) = 1 per (year, material, region, modeled_end_use).
constraints:
  - "If a detailed mapping is introduced, shares must sum to 1 within each (year, material, region)."
