# Quality axis used by the process graph. Keep minimal unless quality split is modeled.
dimension:
  name: quality
  symbol: q

# `mixed` means no explicit grade split is currently simulated.
qualities:
  - mixed
