# Scenario config: recycling_disruption
# Practical reading order: implementation -> global blocks -> dimension_overrides.
# Keep overrides minimal and aligned with the mechanism being tested.
name: recycling_disruption
# Short plain-language summary used in reporting and run metadata.
description: Global recycling-system disruption with explicit regional severity
  overrides across EU27, China, and RoW.
# Mechanism checklist. These bullets should match the actual overrides below.
implementation:
- Applies a global recycling disruption during 2025-2039.
- Regional overrides for EU27, China, and RoW tune disruption depth and trade/circular-routing pressure.
# Shock events use step parameters: start_year, duration_years, multiplier.
shocks:
  recycling_disruption:
    # Activation year for this temporal override/event.
    start_year: 2025
    # Event duration in years (inclusive step window).
    duration_years: 15
    # Multiplies the baseline channel while the event is active.
    multiplier: 0.8
# Slice-targeted overrides. Matching items are applied in the listed order.
dimension_overrides:
# Override item: define scope first, then local block deltas.
- name: eu27_disruption
  # Region filter for this override item. Omit to match all regions.
  regions:
  - EU27
  shocks:
    recycling_disruption:
      # Activation year for this temporal override/event.
      start_year: 2025
      # Event duration in years (inclusive step window).
      duration_years: 15
      # Multiplies the baseline channel while the event is active.
      multiplier: 0.55
    trade_refined_import_need_multiplier:
      # Activation year for this temporal override/event.
      start_year: 2025
      # Event duration in years (inclusive step window).
      duration_years: 15
      # Multiplies the baseline channel while the event is active.
      multiplier: 0.75
    collection_rate:
      # Activation year for this temporal override/event.
      start_year: 2025
      # Event duration in years (inclusive step window).
      duration_years: 15
      # Multiplies the baseline channel while the event is active.
      multiplier: 0.92
    # Routing-triad note: keep recycling + remanufacturing + disposal normalized.
    recycling_rate:
      # Activation year for this temporal override/event.
      start_year: 2025
      # Event duration in years (inclusive step window).
      duration_years: 15
      # Multiplies the baseline channel while the event is active.
      multiplier: 0.88
    # Routing-triad note: keep recycling + remanufacturing + disposal normalized.
    remanufacturing_rate:
      # Activation year for this temporal override/event.
      start_year: 2025
      # Event duration in years (inclusive step window).
      duration_years: 15
      # Multiplies the baseline channel while the event is active.
      multiplier: 0.92
# Override item: define scope first, then local block deltas.
- name: china_disruption
  # Region filter for this override item. Omit to match all regions.
  regions:
  - China
  shocks:
    recycling_disruption:
      # Activation year for this temporal override/event.
      start_year: 2025
      # Event duration in years (inclusive step window).
      duration_years: 15
      # Multiplies the baseline channel while the event is active.
      multiplier: 0.65
    trade_refined_import_need_multiplier:
      # Activation year for this temporal override/event.
      start_year: 2025
      # Event duration in years (inclusive step window).
      duration_years: 15
      # Multiplies the baseline channel while the event is active.
      multiplier: 0.82
    collection_rate:
      # Activation year for this temporal override/event.
      start_year: 2025
      # Event duration in years (inclusive step window).
      duration_years: 15
      # Multiplies the baseline channel while the event is active.
      multiplier: 0.94
    # Routing-triad note: keep recycling + remanufacturing + disposal normalized.
    recycling_rate:
      # Activation year for this temporal override/event.
      start_year: 2025
      # Event duration in years (inclusive step window).
      duration_years: 15
      # Multiplies the baseline channel while the event is active.
      multiplier: 0.9
    # Routing-triad note: keep recycling + remanufacturing + disposal normalized.
    remanufacturing_rate:
      # Activation year for this temporal override/event.
      start_year: 2025
      # Event duration in years (inclusive step window).
      duration_years: 15
      # Multiplies the baseline channel while the event is active.
      multiplier: 0.94
# Override item: define scope first, then local block deltas.
- name: row_disruption
  # Region filter for this override item. Omit to match all regions.
  regions:
  - RoW
  shocks:
    recycling_disruption:
      # Activation year for this temporal override/event.
      start_year: 2025
      # Event duration in years (inclusive step window).
      duration_years: 15
      # Multiplies the baseline channel while the event is active.
      multiplier: 0.75
    trade_refined_import_need_multiplier:
      # Activation year for this temporal override/event.
      start_year: 2025
      # Event duration in years (inclusive step window).
      duration_years: 15
      # Multiplies the baseline channel while the event is active.
      multiplier: 0.88
    collection_rate:
      # Activation year for this temporal override/event.
      start_year: 2025
      # Event duration in years (inclusive step window).
      duration_years: 15
      # Multiplies the baseline channel while the event is active.
      multiplier: 0.96
    # Routing-triad note: keep recycling + remanufacturing + disposal normalized.
    recycling_rate:
      # Activation year for this temporal override/event.
      start_year: 2025
      # Event duration in years (inclusive step window).
      duration_years: 15
      # Multiplies the baseline channel while the event is active.
      multiplier: 0.93
    # Routing-triad note: keep recycling + remanufacturing + disposal normalized.
    remanufacturing_rate:
      # Activation year for this temporal override/event.
      start_year: 2025
      # Event duration in years (inclusive step window).
      duration_years: 15
      # Multiplies the baseline channel while the event is active.
      multiplier: 0.96
