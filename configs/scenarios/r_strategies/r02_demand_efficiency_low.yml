# Scenario config: r02_demand_efficiency_low
# Practical reading order: implementation -> global blocks -> dimension_overrides.
# Keep overrides minimal and aligned with the mechanism being tested.
name: r02_demand_efficiency_low
# Short plain-language summary used in reporting and run metadata.
description: Low-intensity R0/R2 pathway combining moderate demand reduction and material-efficiency
  gains.
# Mechanism checklist. These bullets should match the actual overrides below.
implementation:
- Activate demand-transformation with modest service/activity-intensity shift and
  efficiency gains.
- Improve fabrication yield to represent material efficiency.
- Use regional overrides for differentiated ambition.
# MFA-side physical levers (collection, yields, loss controls, and stock constraints).
mfa_parameters:
  fabrication_yield:
    # Activation year for this temporal override/event.
    start_year: 2025
    value: 0.94
# Demand transformation decomposes demand into service-activity and material-intensity channels.
demand_transformation:
  enabled:
    # Activation year for this temporal override/event.
    start_year: 2020
    value: true
    before: false
  service_activity_source: service_activity
  material_intensity_source: material_intensity
  service_activity_multiplier:
    # Activation year for this temporal override/event.
    start_year: 2025
    value: 1.01
  material_intensity_multiplier:
    # Activation year for this temporal override/event.
    start_year: 2025
    value: 0.95
  efficiency_improvement:
    # Activation year for this temporal override/event.
    start_year: 2025
    value: 0.04
  rebound_effect: 0.2
  transition_adoption_weight: 0.0
  min_demand_multiplier: 0.75
  max_demand_multiplier: 1.15
# Slice-targeted overrides. Matching items are applied in the listed order.
dimension_overrides:
# Override item: define scope first, then local block deltas.
- name: eu27_r02_low
  # Region filter for this override item. Omit to match all regions.
  regions:
  - EU27
  mfa_parameters:
    fabrication_yield:
      # Activation year for this temporal override/event.
      start_year: 2025
      value: 0.945
  demand_transformation:
    service_activity_multiplier:
      # Activation year for this temporal override/event.
      start_year: 2025
      value: 1.0
    material_intensity_multiplier:
      # Activation year for this temporal override/event.
      start_year: 2025
      value: 0.93
    efficiency_improvement:
      # Activation year for this temporal override/event.
      start_year: 2025
      value: 0.05
# Override item: define scope first, then local block deltas.
- name: china_r02_low
  # Region filter for this override item. Omit to match all regions.
  regions:
  - China
  mfa_parameters:
    fabrication_yield:
      # Activation year for this temporal override/event.
      start_year: 2025
      value: 0.94
  demand_transformation:
    service_activity_multiplier:
      # Activation year for this temporal override/event.
      start_year: 2025
      value: 1.03
    material_intensity_multiplier:
      # Activation year for this temporal override/event.
      start_year: 2025
      value: 0.95
    efficiency_improvement:
      # Activation year for this temporal override/event.
      start_year: 2025
      value: 0.035
# Override item: define scope first, then local block deltas.
- name: row_r02_low
  # Region filter for this override item. Omit to match all regions.
  regions:
  - RoW
  mfa_parameters:
    fabrication_yield:
      # Activation year for this temporal override/event.
      start_year: 2025
      value: 0.937
  demand_transformation:
    service_activity_multiplier:
      # Activation year for this temporal override/event.
      start_year: 2025
      value: 1.04
    material_intensity_multiplier:
      # Activation year for this temporal override/event.
      start_year: 2025
      value: 0.97
    efficiency_improvement:
      # Activation year for this temporal override/event.
      start_year: 2025
      value: 0.03
